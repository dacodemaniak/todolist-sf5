{% extends 'base.html.twig' %}

{% block title %}Détail{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Tâche</h1>

 	<h2>{{ task.title }}</h2>
 	<blockquote>
 		{{ task.summary }}
 	</blockquote>
 	Créée le : {{ task.createdAt | date("d-m-Y") }}
 	
 	<a href="{{ path("task") }}">Retour à la liste</a>
</div>
{% endblock %}
